#include <stdio.h>
#include <math.h>

int     main()
{
  int   entier;
  int   resultat;
  int   test;
  
  resultat = 1;
  test = 1;
  
  scanf("%d", &entier);

  while (resultat * 2 <= entier)
    resultat *= 2;
  
  while (test != resultat)
    {
      pow(test, i);
      i++;
      


  printf("%d", resultat);

  return 0;
}
